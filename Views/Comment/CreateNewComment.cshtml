@model DreamTeamBlogZ.Models.Database.Comment

<br />
@*<b>Skapa nytt inlägg i @ViewBag.PostTitle</b>*@
<br />
<br />


@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()

    <div class="row">
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="col-md-12">
            <b>Skapa en kommentar: </b>
            <br />
            <div class="col-md-pull-8">
                @Html.EditorFor(model => model.Body, new { htmlAttributes = new { @class = "form-control" } })
            </div>
            <input type="submit" value="Spara kommentaren" class="btn btn-group-sm col-md-3" />
        </div>

        @{
            int blogId = @ViewBag.BlogId;
            
        }
        @Html.Hidden("Blog", blogId)

        @{
            int postId = @ViewBag.PostId;
        }
        @Html.Hidden("Post", postId)

        <div>
            @{
                string tmp1 = "ShowBlog/";
                string tmp2 = @ViewBag.PostId.ToString();
                string tmp3 = @ViewBag.BlogId.ToString();
                string tmp12 = tmp1 + tmp2;
            }
        </div>
    </div>
}


@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
